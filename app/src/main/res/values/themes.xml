<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BookReader4U" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BookReader4U" parent="Base.Theme.BookReader4U" />

    <style name="Theme.BookReader4U.Fullscreen" parent="Theme.BookReader4U">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>